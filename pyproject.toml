[tool.poetry]
packages = [
    { include = "poetry-linnaeus" }
]
name = "poetry-linnaeus"
version = "0.1.0"
description = ""
authors = ["Fauve Wevers <fauve.wevers@uwv.nl>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
gradio = ">=5.12.0"
python-dotenv = "^1.0.1"
openai = ">=1.59.9"
groq = ">=0.15.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"